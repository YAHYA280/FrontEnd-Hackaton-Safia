scan the condition3.txt file it contains the file that we will expect from the back end , in the current form if the Generate additional preference fields (AI-powered) is checked true , show instead of  generate my internaty , replace it with personlize more your trip , so the back end expect us to send the form with true so it will resend us the dynamyc which has the body exactly like the condotion3.txt file so inspire from the current form display look and generate me a some sort of multi step form showing question after question until it show all the question to the users with suinvat button to show next question one the dynamic form is filled then you should show a loading screen i have it in the compoentn folder until we get a response from the back end so we can redirect the user to the chat page with it's trip information , so the focus now will be on the setup of the dynamic form